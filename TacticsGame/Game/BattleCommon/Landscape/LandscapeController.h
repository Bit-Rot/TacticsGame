#ifndef LANDSCAPECONTROLLER_H
#define LANDSCAPECONTROLLER_H

//Forward-declare Landscape
class Landscape;

/**
 *	The <pre>LandscapeController</pre> is the controller component to the <pre>Landscape</pre> class.  Currently
 *	not put to any significant use, but we'll keep this basic skeleton here in case 
 */
class LandscapeController
{
public:
	LandscapeController();
	~LandscapeController();

protected:

private:

};

#endif //#ifndef LANDSCAPECONTROLLER_H